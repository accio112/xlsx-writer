import { XlsxWriterDirective } from './xlsx-writer.directive';

describe('XlsxWriterDirective', () => {
  it('should create an instance', () => {
    const directive = new XlsxWriterDirective();
    expect(directive).toBeTruthy();
  });
});
